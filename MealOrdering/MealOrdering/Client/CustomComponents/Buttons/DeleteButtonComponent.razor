@inherits BaseButtonComponent


<ButtonComponent Caption="@(Caption ?? "Delete")"
                 Color="@(Color ?? "danger")"
                 Icon="@(Icon ?? "trash-o")"
                 OnClick="OnClick"
                 Tooltip="@(Tooltip ?? "Delete")"
                 IsOutline="@IsOutline"
                 IsSmall="@IsSmall" />
